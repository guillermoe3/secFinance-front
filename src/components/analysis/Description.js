import React from 'react'

function Description({text}) {
    return (
        <div>
            {console.log(text)}
            {text}
            
        </div>
    )
}

export default Description
